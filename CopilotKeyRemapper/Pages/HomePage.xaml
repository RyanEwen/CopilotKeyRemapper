<Page
    x:Class="CopilotKeyRemapper.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer Padding="32,16,32,32">
        <StackPanel Spacing="16" MaxWidth="760" HorizontalAlignment="Left">
            <TextBlock Text="Copilot Key Remapper" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="Choose what your keyboard's Copilot key does." Opacity="0.7" Margin="0,-8,0,0" />

            <InfoBar x:Name="StatusBar" IsOpen="True" IsClosable="False">
                <InfoBar.ActionButton>
                    <Button x:Name="AssignButton" Content="Assign in Windows Settings" Click="AssignButton_Click" />
                </InfoBar.ActionButton>
            </InfoBar>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                    CornerRadius="8" Padding="20">
                <StackPanel Spacing="14">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <FontIcon Glyph="&#xE765;" FontSize="22" VerticalAlignment="Center" />
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="When the Copilot key is pressed" Opacity="0.7" FontSize="12" />
                            <TextBlock x:Name="ActionText" Style="{StaticResource SubtitleTextBlockStyle}" />
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:Name="ChangeButton" Click="ChangeButton_Click" Style="{StaticResource AccentButtonStyle}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE70F;" FontSize="14" />
                                <TextBlock Text="Change action" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="TestButton" Click="TestButton_Click">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE768;" FontSize="14" />
                                <TextBlock Text="Test it now" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
